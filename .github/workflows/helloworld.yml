name: Hello world workflow

# controls when the action will run. Trigger the Workflow on push to master or production branch 
on: 
  push:
    branches:
      - master
      - production 

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:  
      - name: print hello_world
        run: echo "hello_world"